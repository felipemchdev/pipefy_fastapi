services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////app/data/app.db
      - PIPEFY_API_URL=https://api.pipefy.com/graphql
      - PIPEFY_API_TOKEN=
      - LOG_LEVEL=INFO
      - DEBUG=false
    volumes:
      - ./data:/app/data
